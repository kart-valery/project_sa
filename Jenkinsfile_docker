pipeline {
   agent any
   stages {
    stage('Clone Repo') {
      steps {
      steps{
        git url: 'https://github.com/kart-valery/project_sa.git',
          credentialsId: 'kart_git_hub'
      }
    }
    stage('Install Docker (playbook)'){
      steps{
        ansiblePlaybook credentialsId: 'jenkins_node', disableHostKeyChecking: true, installation: 'ansible', inventory: 'ansible/inventory.yaml', playbook: 'ansible/inst_docker.yaml'
      }
    }
  }
    post {
        always {
            deleteDir()
        }
    
  }
}
